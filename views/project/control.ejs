<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head", { pageTitle: "ABSOLUTE CONTROL" }); %>
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<body>
   <% if (user !== "(not signed in)") { %>
         <div class="layout dark:bg-gray-900 dark:text-gray-100">
         <%- include("../partials/navbar"); %>

         <div class="page">
            <div class="content grid" style="grid-template-columns: 30% 70%">
               <div class="max-h-[90vh] overflow-auto bg-gray-800 rounded-md">
                  <% JSON.parse(project).data.forEach((card) => { %>
                     <div onclick="showCard(<%= card %>)" class="p-2 px-4 m-2 bg-gray-700 rounded-lg">
                        <p class="text-lg"><%= card.name %></p>
                        <p class="cardDesc text-gray-300"><%= card.about %></p>
                     </div>
                  <% }); %>
                  <p class="mb-3 text-center text-gray-500">And that's the end!</p>
                  <style>
                     .cardDesc {
                        text-overflow: ellipsis;
                        overflow: hidden;
                        white-space: nowrap;
                     }
                  </style>
               </div>
               <div class="activeCard">
                  <p>💖💖💖 Place holder content! 💖💖💖</p>
               </div>
            </div>
         </div>
      </div>
      <%- include("../partials/locbar", { text: "Home>ABSOLUTE CONTROL" }); %>
   <%} else { %>
      <%- include("../partials/signedOut"); %>
      <%- include("../partials/signIn"); %>
   <% } %>
</body>
<style>
.gradient-project-overview {
   background: linear-gradient(to bottom right, #82b1ff, #2962ff);
   -webkit-background-clip: text;
   -webkit-text-fill-color:transparent;
   background-clip: text;
}
</style>
<script>let cardArray = <%= JSON.stringify(project).data %></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
function showCard(card) {
   console.log(card);
}
</script>
</html>