<div class="signin-modal hidden fixed top-0 bottom-0 right-0 left-0 justify-center items-center">
   <div class="signin-modal-inner shadow-md dark:shadow-black text-center bg-white dark:bg-gray-900 rounded-3xl">
      <h2 class="my-2 text-[#222] text-3xl font-sans dark:text-gray-200">Sign In</h2>
      <p class="signin-text mb-4 text-[#444] font-nuntio text-2xl dark:text-gray-500">Welcome back!</p>
      <form action="user/login" method="POST" id="signin-from-homepage">
         <input class="input-text mb-1 inline-block w-full shadow-inner dark:shadow-black bg-white dark:bg-gray-800" type="text" name="name" placeholder="Name" required><br>
         <input class="input-text inline-block w-full shadow-inner dark:shadow-black bg-white dark:bg-gray-800" type="password" name="pscd" placeholder="Password" required><br>
         <input class="inline-block w-full -mb-8 bg-gradient-to-br from-amber-400 to-orange-400 shadow-lg dark:shadow-none shadow-orange-600 px-5 py-3 my-2.5 text-white text-lg font-nunito font-semibold rounded-xl" type="submit" value="Enter Account">
      </form>
      <button class="inline-block w-full mt-5 mb-2 bg-amber-400 dark:bg-amber-500 shadow-lg dark:shadow-none shadow-amber-600 px-5 py-3 my-2.5 text-white text-lg font-nunito font-semibold rounded-xl" onclick="document.querySelector('.signup-modal').style.display = 'flex'; document.querySelector('.signin-modal').style.display = 'none'">Create Account</button>
   </div>
</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
$("#signin-from-homepage").submit(function(event) {
   event.preventDefault();
   $.post("/user/login", {
      name: this.name.value,
      pscd: this.pscd.value,
   }).done(function(data) {
      document.querySelector(".signin-text").textContent = data;
      if (data == "Success!") { location.reload(); }
   });
});
</script>